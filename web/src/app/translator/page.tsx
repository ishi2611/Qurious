import Translator from "@/components/translator/Translator";

export const metadata = { title: "Classical → Quantum Translator" };

export default function TranslatorPage() {
  return <Translator />;
}
